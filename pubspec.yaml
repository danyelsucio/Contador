dependencies:
  image_picker: ^1.0.7        # Cámara y galería
  fast_paddle_ocr: ^0.3.0     # ⭐ OCR offline + UI de cámara integrada
  permission_handler: ^11.3.0 # Permisos
  path_provider: ^2.1.2       # Directorios locales
  path: ^1.9.0                # Manejo de rutas
